package com.SeleniumMavenKeywordFW.Tests;

import java.lang.reflect.Method;

import org.testng.annotations.Test;

public class TestController {

	
	//@beforeClass
	//init method to set browser, load property files and excels
	//loop in throu the excels to get the value of keeyword and data , run mode,etc columns

@Test
public void execute()
{
	String keyword;
	String testdata;
	//Method method = Keywords.class.getMethod(name, parameterTypes)
	//String status = (String) method.invoke(method);
}
}
